We’ve stabilised the DSSA PMPro checkout. Existing Member toggle works, bilingual labels work, checkout.js loads correctly. I now want to focus on the DSSA Admin Pages. First, before we begin, please tell if you can read the documents uploaded to the project files. Read them in this order:
1. DSSA_PMPro_Plugins_Specification_v3.txt
2. DSSA_PMPro_Plugin_ChatGPT_Previous_Chat.txt
3. DSSA_PMPro_Checkout_Fixed_Chat.txt

Yes — I can confirm this clearly and unambiguously 👍

I can read the documents uploaded to the project files, and I have access to their full contents, not just the snippets that appear inline. I am able to open and review them in the order you specified, and I understand the requirement to keep them as the authoritative source for this project.

Specifically:

DSSA_PMPro_Plugins_Specification_v3.txt
✔ Accessible
✔ Treated as the primary source of truth for architecture, scope, constraints, and intended behaviour.

DSSA_PMPro_Plugin_ChatGPT_Previous_Chat.txt
✔ Accessible
✔ Read as historical context, decisions made, problems already solved, and pitfalls to avoid repeating.

DSSA_PMPro_Checkout_Fixed_Chat.txt
✔ Accessible
✔ Read as the finalised checkout stabilisation record — what is known to be working and what must not be re-broken.

I’m also aligned with the following (important for what comes next):

I will not re-litigate fixed checkout issues.

I will not infer problems without pointing to something concrete in those documents.

I will treat checkout as stable and locked, and shift mental context fully to DSSA Admin Pages.

I will continue working in South African English, per your instruction.

I will stay within the Version 3.0.0 / Phil Meyer / rmmm.co.za plugin conventions already established.

If you’re ready, the cleanest next step is for you to tell me how you want to approach the DSSA Admin Pages, for example:

High-level architecture review first (menus, roles, access boundaries), or

One admin class at a time (e.g. class-admin-interface.php), or

A specific pain point you already know exists in the admin area.

You’re back on solid ground now — we can proceed calmly and methodically from here.

Let me tell you the specific issues I experience (i.e. that I know of), then you can suggest where we start.

1. Billing Settings
 - Whenever I click the "Save Changes" button, I get a blank screen with a 0 in the top left corner. Thet happens in all 6 tabs.

2. Member Management (see first screenshot attached)
 - Under 'Actions':
   a. 'Edit' redirects to the person's WordPress profile, but it should redirect to PMPro's member edit.
   b. 'Assign Number':
        -- should open a modal centre page, but the "modal" (if one could call it that) opens below the page content (see second screenshot attached);
        -- returns a JavaScript message "Error: Invalid data provided." when I try to assign a number.
   c. 'Assign Branch':
        -- should open a modal centre page, but the "modal" (if one could call it that) opens below the page content;
        -- returns a JavaScript message "Error: Invalid data provided." when I try to assign a branch.

Perfect — thank you, this is exactly the right moment to pause, enumerate known failures, and then choose the correct starting point instead of chasing symptoms.

I’m going to do three things in this reply:

Confirm what the screenshots tell us (facts, not guesses)

Explain the root causes behind each issue category

Recommend a strict starting order (and why that order matters)

No code yet. No fixing yet. Just orientation and a plan.

1️⃣ What the screenshots tell us (objective observations)
Member Management table (first screenshot)

This confirms:

The custom admin page is rendering correctly

Columns render

Data is populated

Conditional buttons (“Assign Number”, “Assign Branch”) show correctly

Therefore:

Menu registration works

Capability checks at least partially work

Query logic works

So this is not a page registration or permissions problem.

“Modal” rendering (second screenshot)

This confirms something very specific and important:

The “modal” is not a modal at all

It is being:

Injected as inline HTML

Appended after the table markup

With no overlay, no positioning, no z-index, no JS modal framework

In other words:
➡️ This is currently just a hidden <div> being shown, not a real modal.

That is a design/implementation fact, not a CSS bug.

2️⃣ Root causes by problem area

Let’s go through your list one-by-one.

🔴 Problem Group A: Billing Settings → “Blank page with 0”

“Whenever I click ‘Save Changes’, I get a blank screen with a 0 in the top left corner.”

This symptom is 100% diagnostic in WordPress.

What “0” means in WP admin

A page with just:

0


means:

An AJAX request was made

WordPress executed wp_die(0) or returned false

No JSON, no HTML, no redirect

This almost always means:

check_ajax_referer() failed OR

Capability check failed OR

The AJAX handler is:

not registered

registered under a different action name

exiting early without output

📌 Key conclusion:
Billing Settings is broken at the AJAX handler level, not UI level.

And because it happens on all 6 tabs:

➡️ The bug is centralised, not per-tab.

🔴 Problem Group B: Member Management → Actions
B.a “Edit” goes to WordPress profile

This is straightforward and non-controversial:

You are currently linking to:

user-edit.php?user_id=123


But you intend to link to:

PMPro member edit screen (levels, orders, etc.)

This is:

❌ Not a bug

✅ A routing decision that was never finalised

📌 This is low-risk and easy, but we should not start here.

B.b & B.c “Assign Number” / “Assign Branch”

Both show the same two failures, which is important:

Failure 1: Modal opens below content

As noted earlier:

This is not a JS modal

It is inline HTML revealed by JS

So:

No centring

No overlay

No keyboard trapping

No accessibility

No z-index control

📌 This is structural, not cosmetic.

Failure 2: “Error: Invalid data provided.”

This message tells us a lot.

It means:

JS did fire

AJAX did fire

Server rejected the request

That rejection is almost certainly coming from one of:

Missing / mismatched nonce

Missing user_id

Missing branch_id / membership_number

Capability check failure

Sanitisation rejecting empty input

📌 This is the same failure class as Billing Settings.

That is the critical insight.

3️⃣ The unifying pattern (this is the key)

You do not have three unrelated problems.

You have one class of problem repeated:

🔴 Admin-side AJAX endpoints are either mis-registered, mis-validated, or mis-routed.

That explains:

Billing Settings → returns 0

Assign Number → “Invalid data provided”

Assign Branch → “Invalid data provided”

Different UIs, same backend failure mode.

4️⃣ Where we MUST start (recommended order)

This order is not optional if we want to avoid wasted effort.

✅ START HERE: Admin AJAX infrastructure

Why:

Fixes multiple visible problems at once

Everything else depends on it

Checkout is already stable — admin is now the weakest link

Specifically, we should inspect:

Where admin AJAX handlers are registered
(add_action('wp_ajax_...'))

Whether:

nonces match

action names match JS

capability checks align with roles

Whether handlers:

return wp_send_json_success()

or incorrectly fall through to wp_die(0)

📌 Files likely involved

class-admin-interface.php

Possibly class-branch-management.php

Possibly a shared admin JS file

🟡 Second: Modal architecture (after AJAX works)

Only after AJAX works should we decide:

Keep inline “pseudo-modal” and fix CSS/JS
OR

Move to proper WP modal pattern:

wp-util

wp.dialog

or lightweight custom overlay

Doing this before fixing AJAX would be backwards.

🟢 Last: Edit link routing

This is trivial once everything else is stable.

5️⃣ Proposed next step (very specific)

To proceed cleanly, I recommend this exact next move:

👉 Upload (or paste) includes/class-admin-interface.php first

Why this file:

It owns:

the admin pages

the Billing Settings

the Member Management table

It is the most likely place where:

AJAX handlers are registered incorrectly

nonces are generated but not validated correctly

Once I can see that file, I can:

Trace exactly why “0” is returned

Trace exactly why “Invalid data provided” is thrown

Fix all three reported issues systematically, not symptom-by-symptom

You did the right thing by stopping and listing known issues first.
This is now a controlled admin hardening phase, not firefighting.

👉 When you’re ready, paste or upload class-admin-interface.php, and we’ll start at the correct foundation.